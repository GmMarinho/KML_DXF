############################################################
# Requisitos do projeto KML_DXF
# Ambiente alvo: Python >= 3.11 (testado em 3.13)
# Nota: versões fixas para reprodutibilidade. Ajuste conforme necessário.
############################################################

# --- Core HTTP / Parsing ---
requests==2.32.5          # chamadas à API OpenTopoData
simplekml==1.3.6          # mantido (parser atual é XML manual; pode ser removido depois)
ezdxf==1.4.2              # geração de DXF
shapely==2.1.1            # geom (futuras validações / buffers / etc.)
pandas==2.3.3             # exportações tabulares futuras

# --- Elevação / Geo / ML ---
scikit-learn==1.7.2       # clustering (DBSCAN) para reduzir requisições
pyproj==3.7.2             # projeção Lat/Lon -> UTM

# Dependências científicas explicitadas (eram apenas transitivas, agora pinadas)
numpy==2.3.3              # base numérica (usada por scikit-learn e projeções)
scipy==1.16.2             # requerido por scikit-learn
joblib==1.5.2             # utilitário de paralelização (sklearn)
threadpoolctl==3.6.0      # controle de threads (sklearn)

# --- UX / CLI ---
tqdm==4.66.5              # barra de progresso

# --- Testes ---
pytest==7.4.0             # suíte de testes

# --- Qualidade (instalar manualmente se quiser) ---
# ruff==0.6.9
# flake8==7.1.1

############################################################
# Observações:
# - Se quiser reduzir tamanho: remover simplekml e shapely se não forem usados.
# - Para builds estritos, gerar um constraints.txt com hashes (pip-compile).
# - Atualizações de scikit-learn podem exigir ajuste nas versões de numpy/scipy.
############################################################
